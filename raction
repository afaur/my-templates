//import axios from 'axios'
//const ROOT_URL = 'http://someapi.com/'
//const API_KEY = 'abc123'

const THING_SELECTED = 'THING_SELECTED'
const THING_FETCH = 'THING_FETCH'

const selectThing = function(thing) {
  return {
    type: THING_SELECTED,
    payload: thing,
  }
}

const fetchThing = function(thing) {
  //const url = `${ROOT_URL}?thing=${thing}&api_key=${API_KEY}`
  //const request = axios.get(url)

  return {
    //type: THING_FETCH,
    //payload: request,
  }
}

export {
  actionThing, fetchThing,
  THING_SELECTED, THING_FETCH,
}
# cursor: 28 del
